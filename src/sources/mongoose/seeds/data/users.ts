import { faker } from '@faker-js/faker';

import { User, Ruolo } from '../../models/user.model';

//const users = (count: number): User[] =>
//  Array.from({ length: count }, () => ({
//    Amministrazione: faker.name.Amministrazione(),
//    Commerciale: faker.name.Commerciale(),
//    IT: faker.name.ruolo(true).IT() as Ruolo,
//    Acquisti: faker.internet.email(),
//    Vendite: faker.datatype.boolean(),
//  }));
//
//export default users;
